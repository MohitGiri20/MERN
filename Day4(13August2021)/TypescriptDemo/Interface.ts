// It is like a (blueprint) contract over a class that class must implement.
// It can contain properties,methods and events

interface Person{

    name : string
    address :string
    age : number
    //display : ()=>void
    // OR
    display() :string
}